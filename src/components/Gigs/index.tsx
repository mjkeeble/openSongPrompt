// import { GIGS } from '../../../data/gigs';
// import { TGig } from '../../../types';
// import {displayDate} from '../../utils';

export const Gigs = () => {
  return (
    <div>
      <h1>Gigs</h1>
      {/* <ul>
        {GIGS.map((gig: TGig) => (
          <li key={gig.id}>
            <button>{displayDate(gig.date)}, {gig.location}</button>
          </li>
        ))}
      </ul> */}
    </div>
  );
};
